// Archivos de Texto 
// Uso FEOF()

#include <stdio.h>
#include <stdlib.h>

int main ()
{

	FILE * FP;
	
/*	if ( (FP = fopen("arch02.exe", "rb")) == NULL ) {
		printf("\n\n Error apertura de archivo \n\n");	
		exit(1);
	}; */

	fprintf( stdout, "\nAutor: Luz Lloveras" );

	
//	fclose(FP);
		
	return 0;
}
